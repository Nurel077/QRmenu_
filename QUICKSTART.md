# 🚀 Быстрый старт RestaurantQR

## Самый быстрый способ запуска (5 минут)

### Вариант 1: С Docker (Рекомендуется)

```bash
# 1. Клонировать проект
git clone <repository-url>
cd restaurant_qr_project

# 2. Создать .env файл
cp .env.example .env

# 3. Запустить все сервисы
docker-compose up -d

# 4. Выполнить миграции
docker-compose exec web python manage.py migrate

# 5. Создать суперпользователя
docker-compose exec web python manage.py createsuperuser

# 6. Загрузить тестовые данные
docker-compose exec web python manage.py shell < scripts/generate_sample_data.py

# 7. Готово! Открыть в браузере:
# http://localhost/
```

### Вариант 2: Без Docker (10 минут)

```bash
# 1. Клонировать проект
git clone <repository-url>
cd restaurant_qr_project

# 2. Создать виртуальное окружение
python -m venv venv
source venv/bin/activate  # Linux/Mac
# venv\Scripts\activate   # Windows

# 3. Установить зависимости
pip install -r requirements.txt

# 4. Создать .env файл
cp .env.example .env
# Для начала можно использовать SQLite, ничего не меняя в .env

# 5. Выполнить миграции
python manage.py migrate

# 6. Создать суперпользователя
python manage.py createsuperuser

# 7. Загрузить тестовые данные
python manage.py shell < scripts/generate_sample_data.py

# 8. Запустить сервер
python manage.py runserver

# 9. В другом терминале запустить Redis (для WebSockets)
redis-server

# 10. В третьем терминале запустить Daphne (для WebSockets)
source venv/bin/activate
daphne -b 127.0.0.1 -p 8001 config.asgi:application

# 11. Открыть в браузере:
# http://127.0.0.1:8000/
```

## 📱 Первые шаги после запуска

### 1. Войти в админ-панель

```
URL: http://localhost/admin/
Login: admin
Password: admin123
```

Здесь вы можете:
- Управлять ресторанами
- Добавлять столики
- Редактировать меню
- Просматривать заказы

### 2. Протестировать как гость

```bash
# Получить QR код столика
# В админке: Restaurants → Tables → выбрать столик → скачать QR

# Или открыть напрямую URL столика:
http://localhost/table/test-restaurant/A1/
```

Что можно сделать:
- Просмотреть меню
- Добавить блюда в заказ
- Оформить заказ
- Выбрать способ оплаты

### 3. Протестировать панель официанта

```
URL: http://localhost/waiter/
Login: waiter1
Password: waiter123
```

Что можно делать:
- Просматривать новые заказы
- Подтверждать заказы
- Изменять статусы
- Обрабатывать платежи

### 4. Протестировать панель владельца

```
URL: http://localhost/owner/
Login: owner1
Password: owner123
```

Что доступно:
- Управление меню
- Управление столиками
- Просмотр статистики
- Настройки ресторана

## 🧪 Тестовые данные

После запуска скрипта `generate_sample_data.py` у вас будет:

**Пользователи:**
- Superadmin: `admin` / `admin123`
- Owner: `owner1` / `owner123`
- Waiter 1: `waiter1` / `waiter123`
- Waiter 2: `waiter2` / `waiter123`

**Ресторан:**
- Название: "Тестовый Ресторан"
- 10 столиков (A1-A10)
- 20+ блюд в меню
- 6 категорий меню

## 📚 Основные URL

| Назначение | URL |
|-----------|-----|
| Главная страница | http://localhost/ |
| API документация | http://localhost/api/docs/ |
| Админ-панель | http://localhost/admin/ |
| API | http://localhost/api/ |
| Столик (пример) | http://localhost/table/test-restaurant/A1/ |
| Панель официанта | http://localhost/waiter/ |
| Панель владельца | http://localhost/owner/ |

## 🔧 Полезные команды

### Django Management

```bash
# Создать новую миграцию
python manage.py makemigrations

# Применить миграции
python manage.py migrate

# Создать суперпользователя
python manage.py createsuperuser

# Запустить shell
python manage.py shell

# Собрать статику
python manage.py collectstatic

# Запустить тесты
python manage.py test
```

### Docker

```bash
# Просмотр логов
docker-compose logs -f web

# Перезапуск сервиса
docker-compose restart web

# Остановка всех сервисов
docker-compose down

# Пересборка образов
docker-compose build

# Выполнение команды в контейнере
docker-compose exec web python manage.py <command>
```

### Celery (для фоновых задач)

```bash
# Запуск worker
celery -A config worker -l info

# Запуск beat (планировщик)
celery -A config beat -l info

# Мониторинг задач
celery -A config flower
```

## 🎯 Основные сценарии использования

### Сценарий 1: Гость делает заказ

1. Гость сканирует QR код на столике
2. Открывается меню ресторана
3. Гость выбирает блюда
4. Оформляет заказ с выбором оплаты
5. Получает подтверждение и номер заказа
6. Отслеживает статус в реальном времени

### Сценарий 2: Официант обрабатывает заказы

1. Официант входит в систему
2. Видит список новых заказов
3. Подтверждает заказ (если наличная оплата)
4. Передает заказ на кухню
5. Меняет статус на "Готовится"
6. Когда готово - "Готов к выдаче"
7. Доставляет заказ - "Доставлен"
8. Принимает оплату - "Оплачен"

### Сценарий 3: Владелец управляет рестораном

1. Владелец входит в панель
2. Добавляет новые блюда в меню
3. Загружает фото блюд
4. Устанавливает цены
5. Добавляет новые столики
6. Генерирует QR коды
7. Просматривает статистику продаж

## ❓ FAQ

**Q: Как добавить новый ресторан?**  
A: Войдите в админку как superadmin, перейдите в Restaurants → Add Restaurant

**Q: Как сгенерировать QR коды для всех столиков?**  
A: QR коды генерируются автоматически при создании столика. Скачать можно из админки.

**Q: Как настроить реальную оплату?**  
A: В файле .env настройте STRIPE_SECRET_KEY или другой платежный шлюз в apps/payments/integrations/

**Q: Как включить email уведомления?**  
A: В .env настройте EMAIL_HOST, EMAIL_PORT, EMAIL_HOST_USER и EMAIL_HOST_PASSWORD

**Q: Нужен ли Redis обязательно?**  
A: Да, для WebSockets (real-time обновления заказов). Без него сайт работает, но без реального времени.

**Q: Можно ли использовать PostgreSQL вместо SQLite?**  
A: Да, в .env измените DATABASE_URL на postgresql://...

**Q: Как посмотреть API документацию?**  
A: Откройте http://localhost/api/docs/ - там Swagger UI с полным описанием API

## 🐛 Проблемы и решения

**Проблема: Ошибка импорта модулей**  
Решение: Убедитесь, что виртуальное окружение активировано и все зависимости установлены

**Проблема: Не работают WebSockets**  
Решение: Проверьте, что Redis запущен и Daphne работает на порту 8001

**Проблема: Миграции не применяются**  
Решение: `python manage.py migrate --run-syncdb`

**Проблема: Static файлы не загружаются**  
Решение: `python manage.py collectstatic --clear`

## 📖 Дополнительная документация

- [README.md](README.md) - Основное описание проекта
- [ARCHITECTURE.md](ARCHITECTURE.md) - Архитектура системы
- [API.md](API.md) - Документация API
- [DEPLOYMENT.md](DEPLOYMENT.md) - Развертывание на production
- [PROJECT_STRUCTURE.md](PROJECT_STRUCTURE.md) - Структура проекта
- [TODO.md](TODO.md) - План развития

## 💬 Поддержка

Если у вас возникли проблемы:
1. Проверьте FAQ выше
2. Посмотрите логи: `docker-compose logs -f` или `tail -f logs/django.log`
3. Создайте Issue на GitHub
4. Напишите на email: support@example.com

## 🎉 Готово!

Теперь у вас работает полноценная система QR-меню для ресторанов!

Следующие шаги:
1. Изучите код в `/apps/` директориях
2. Прочитайте [ARCHITECTURE.md](ARCHITECTURE.md)
3. Посмотрите [API.md](API.md) для понимания API
4. Начните разработку новых функций по [TODO.md](TODO.md)

**Приятной разработки! 🚀**
